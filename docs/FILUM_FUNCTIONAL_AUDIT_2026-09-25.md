# FILUM functional audit — 2026-09-25

## Fixed FILUM startup extension URL — 2026-09-26

The packaged policy and branding default now use `moz-extension://5db2d283-fbda-489c-9f1f-f77a0a674080/newtab.html` as the startup homepage. The built-in addon ID `resource-controller@matrixneo23.browser` is assigned that same UUID through the default `extensions.webextensions.uuids` mapping, so a fresh profile can resolve the fixed URL while Firefox can still create a user-level mapping containing subsequently installed addons. Product, wiring, functional, addon lifecycle, syntax, JSON and diff gates pass locally. A new native Windows build and fresh-profile smoke are required before delivery.

## Addon lifecycle correction — 2026-09-26

The FILUM addon page incorrectly used the WebExtensions `management.setEnabled()` path for ordinary extensions. On Firefox that path does not provide reliable enable/disable control for normal WebExtensions, leaving an installed addon without a working re-enable action. Addon enable, disable and uninstall now run through the built-in privileged `AddonManager`, with separate `PERM_CAN_ENABLE`, `PERM_CAN_DISABLE` and `PERM_CAN_UNINSTALL` checks and effective-state readback after every action. The page keeps disabled addons visible, changes the action to **Attiva**, confirms removals, refreshes on lifecycle events, and surfaces failures instead of reporting success optimistically.

The dedicated lifecycle audit exercises active → disabled → enabled → uninstalled and is mandatory in the Windows workflow. Full JavaScript syntax, JSON parsing, Python compilation, product validation, wiring, functional state tests and diff hygiene pass locally. `about:newtab` remains the stable startup target; Firefox resolves it to the profile-specific `moz-extension://<generated-id>/newtab.html`, avoiding an invalid hard-coded UUID. A new Windows build is still required to establish the privileged AddonManager behavior in the packaged Gecko runtime.

## Final Windows verification — run #116

The validated build from complete source commit `0ceb64348c178a2073f326678de0bbcc557a82c7` passed the product gate, native build, and Windows runtime smoke. The smoke checked three repeated GHOST → NORMAL exits, the other modes, controls, Tor 100%, SOCKS5 proxy DNS, WebRTC after a mode switch, real Tor egress (`IsTor=true`), and stop/restore. The final inner ZIP SHA-256, Actions artifact digest, job IDs, detailed scope and remaining user-machine checks are recorded in `releases/FILUM_WINDOWS_X64_RUN_116.md`. Earlier sections below are historical findings and must not be read as the final status of run #116.

## User-machine toolbar failure after run #103

Alberto reports that clicking the visible FILUM button does not open the sidebar. Run #103's self-test dispatched a synthetic `command` event on the button soon after startup; that did not prove a later real click still reaches the same node. The controller bound only the initial toolbarbutton, which CustomizableUI can replace or reparent. The proposed correction delegates `click` and keyboard `command` handling at the document level, deduplicates click-generated commands, and shows a visible error if panel initialization fails. The native self-test now calls `button.click()` rather than constructing a command event. This requires a new native Windows run and a user-machine GUI retest; do not treat the previous passing smoke as proof of this interaction.

## Native Windows run #101 and corrections

Run #102 confirmed the homepage preference readback (`about:newtab`, startup page 1), then stopped at a test-harness error: WebExtensions `tabs.create({url:'about:newtab'})` is rejected as `Illegal URL`. The smoke now opens its own `newtab.html` to check that the page loads. This does not prove that an ordinary first GUI window opens on the branded page; the headless runner cannot supply that visual evidence.

Run #101 built successfully and registered the built-in extension. Native controls passed NORMAL/TURBO/PRIVATE, ADS, DNS preset/custom, network modes, Tor bootstrap (100% in 12 seconds), SOCKS5 remote DNS, WebRTC on mode switch, Tor egress (`IsTor=true`) and stop, and launcher checks. Two checks failed: the headless runner's initial tab was `about:blank`, and Firefox returned `cookieConfig.nonPersistentCookies=false` in GHOST. The latter API field is deprecated and ineffective; GHOST now uses the privileged `network.cookie.noPersistentStorage` preference and verifies its effective value. The startup policy sets `Homepage.URL=about:newtab` and `StartPage=homepage`; the smoke verifies effective startup preferences separately from an explicit `about:newtab` override tab. Headless startup cannot establish the first visible graphical window; that still requires a GUI check on Alberto's machine. Retest the exact new source revision before delivery.

## Additional regression pass (unpublished)

Diagnostics export: the existing sidebar launcher opens `diagnostics.html`. The view now has a Copy diagnostics action and includes mode health, ADS, DNS, HTTPS-only, GHOST recovery and Tor readbacks. It formats labeled rows with a timestamp for pasting into a support conversation. The copied report omits API keys and reduces the proxy object to routing fields. A focused mock exercises report formatting and clipboard writing. Native clipboard permission and rendered layout remain to be checked on Windows.

Tor recovery previously trusted successful proxy/DNS setter calls without checking their effective values. Stop, startup recovery and failed-start rollback now read back both settings; a silent mismatch retains Tor ownership and the warning instead of reporting OFF. Re-entering an already active Tor session also rechecks WebRTC and DoH. ADS activation now verifies the enabled ruleset; an unavailable ruleset read shows `ADS: ERRORE` and a sidebar warning rather than an optimistic ON state. Focused mocked transitions include an ignored proxy write, changed DNS during Tor, and ignored ADS update. Syntax, wiring, functional and product gates pass locally. These checks do not establish actual Tor egress or Gecko behavior on Windows; a native end-to-end run remains required before any build recommendation or publication.

Follow-up functional execution: added an ignored-DNS-write recovery case and reran the functional mock, seven-view wiring gate, product gate, all extension/experiment JavaScript syntax checks, Python compilation, all product JSON parsing, and `git diff --check`: all passed. The current source was not run in a native Windows/Gecko browser; previous CI artifacts were built from older commits and cannot validate this source revision.

Scope: current unpublished `fix/tor-first-bootstrap` checkout based on remote `main` `65b4568`. This is a code-path and local validation audit, not a Windows runtime sign-off. The previously delivered build passed a fast-runner smoke but timed out on Alberto's first Tor bootstrap at 45 seconds.

| Area | Evidence and finding | Disposition |
| --- | --- | --- |
| Modes | Sidebar selects and rereads the saved mode, but the prior smoke only checked the selected button. Privileged and privacy API settings can diverge if a `safeSet` fails or an external setting changes. | Added readback to all four mode smoke checks, manual diagnostics, and a persistent sidebar warning refreshed every five seconds with the mismatched fields. Runtime setter still logs and continues on individual privacy-setting failures, so the warning distinguishes partial activation from complete activation. |
| Mode change with Tor | `set-mode` called `applyRuntimePrivacy`, which enabled WebRTC for NORMAL/TURBO/PRIVATE even while Tor was active. | Fixed: force WebRTC disabled again while Tor is enabled; added Tor plus mode-switch readback to smoke. Requires Windows runtime verification. |
| Tor bootstrap | Hard 45-second timeout killed a process that had reached 50% on Alberto's machine. | Raised timeout to 180 seconds, smoke observation to 195 seconds. Only a user-machine retest can establish whether this is sufficient on his network. |
| Tor routing | The final prior smoke verified Tor 100%, SOCKS5 proxy with proxy DNS, `IsTor=true`, and restoration on stop. | Retain those real checks. New diagnostics reads process, bootstrap, proxy, stage/error/log. It does not claim that local settings prove anonymous traffic. |
| DNS | Presets are official DoH URIs for Cloudflare, Google, Quad9. Existing custom endpoint is kept. | Selection requires Apply; readback checks mode and URI. Added preset smoke. This does not measure which resolver answered a live query. |
| DNS while Tor | A user could re-enable DoH from the sidebar while Tor owned DNS/proxy settings, and Tor stop would restore its earlier DNS snapshot. | Block DNS edits in UI and message handler while Tor is enabled; restore the saved DNS at stop. Tor plus mode switch keeps WebRTC disabled. |
| VPN | SYSTEM/VPN chooses system proxy configuration; it does not establish a VPN tunnel or verify the public exit address. | Do not add provider toggles without a specific client/account integration. Label and diagnostics should continue to avoid claiming a VPN is connected. |
| GHOST cleanup | `browsingData.remove({hostnames})` may remove existing cookies/site storage for visited hosts, while history/form cleanup uses a timestamp. On error, the session record is removed in `finally`, so cleanup cannot be retried. | **Open product decision**: specify whether GHOST may clear pre-existing site data. Recommend a separate ephemeral profile/container for strict isolation, or conservative time-scoped cleanup with an explicit residual-data warning. Do not promise complete ephemeral isolation yet. |
| Resource modes | All four modes allow three active background tabs; TURBO additionally discards eligible tabs idle for 90 seconds. Protected tabs may exceed the limit. | Diagnostics displays actual background count and degradation. Do not claim a universal speed improvement. |
| New diagnostic view | Reads effective prefs, privacy APIs, proxy and Tor status. | Static wiring/syntax checked; native browser rendering and actual values still need the Windows smoke and a manual review. |
| Toolbar F | Replaced tiny full-logo icon with a 24 px vector F in the existing native toolbar button. | Needs visual inspection in Windows at normal scaling. Approved New Tab/background untouched. |
| First window | `chrome_url_overrides.newtab` applies to new tabs, while the first browser window can open `about:home`; CI previously forced `about:blank`. | Set branded startup homepage to `about:newtab` for new/default profiles. CI now launches without a URL and requires the active tab to resolve to FILUM's `newtab.html`. Existing profiles with explicit custom startup preferences may override the branded defaults. |
| Build gate | The full-build Windows smoke read the panel pref before the functional report. Run #100 failed there despite the later smoke-only artifact passing. | Removed the premature on-disk panel-pref gate. The full functional report can only be generated after the native sidebar view opens and its controls run. Keep the strict report gate. |
| Secondary pages | Smart Search, Library, and Addons launchers were checked by URL. Their content, search ranking, mutations, and error handling were not exercised by the prior Windows smoke. | Inspect source and retain launcher gate; add focused runtime scenarios before claiming these pages are functionally complete. |

## Required before publication

1. Run the product gate and JavaScript/schema checks locally.
2. Build the exact committed source once on Windows CI; inspect artifact hash, packaged core, toolbar and diagnostic view.
3. Require mode preference/privacy readbacks, DNS preset/custom readbacks, Tor bootstrap/proxy/egress/stop, and launcher URLs in the Windows functional smoke.
4. Do not declare slow first-run Tor fixed until Alberto tests the new build on the affected machine.

## Open fixes to decide

- P1 partially addressed: the mode now reports effective mismatches in the sidebar, but the setter still catches individual `safeSet` failures. A transient failure followed by another change could be missed until readback; transactional mode activation remains an improvement.
- P1: Define and fix GHOST cleanup semantics before advertising it as a reliably ephemeral session.
- P1 fixed in source: if another extension or user changes the proxy while Tor is running, the primary button now shows `TOR: ERRORE` rather than ON, with a stop affordance. Windows runtime verification remains pending.
- P1: Make changing modes transactional: the current handler stores the requested mode before applying all privacy settings. A later failure could leave the stored selection ahead of the applied state.
- P2: Add a deliberate live DNS resolver verification if the UI should claim a preset is actually being used on the network, rather than merely configured.
- P2: Specify a VPN provider and installed-client integration if FILUM should start and verify VPN connections itself.
- P2: Smart Search collects DuckDuckGo candidates by default. Tavily is an optional second source only on an explicit per-search button with a user-entered key. Default local limit 33/day can be changed by the user, subject to 1000 local calendar-month attempts; provider quota is authoritative. Local ad markers and suspicious URL signals are filtered with reversible hiding. “Fake” content cannot be reliably classified from URL/snippet alone. Verify sampled exclusions, false positives, live Tavily response and rendering before treating the filter as a safety control. The key is stored in the local browser profile and is not protected from someone with filesystem access.

Smart Search now also offers “Segna come fake · escludi sito” on each result. This saves a user-controlled hostname blocklist in local browser storage, immediately hides matching results, and filters the same host and subdomains from future DuckDuckGo/Tavily candidate sets before optional page reads. A remove button restores eligibility on the next explicit search. A mocked add/filter/count/subdomain/remove scenario passed. The label records the user's judgment; it is not an independent truth verdict. Browser rendering and Windows runtime remain unverified.

## Follow-up functional audit (2026-09-25)

- Reviewed search parsing/merging/ranking, user blocklist, explicit Tavily request/quota handling, sidebar status, mode application/readback, Tor transition/restore, diagnostics, and secondary page launch paths. All extension JS parses; manifest/schema JSON parse; product gate and diff hygiene pass.
- Fixed a live-state inconsistency: `get-status` previously returned `torEnabled=false` when the saved Tor session remained active but its process had lost bootstrap. The sidebar then displayed OFF instead of ERROR and offered a new start while the saved proxy could still point to SOCKS. It now reports saved Tor ownership separately from bootstrap/routing and displays a persistent warning plus a stop/recovery affordance. Requires an actual process-exit test in Windows.
- Mode health now checks the configured cookie behavior, WebRTC IP policy, network prediction, and hyperlink auditing as well as the existing fields. GHOST explicitly sets the restrictive WebRTC policy. Mocked healthy NORMAL, changed prediction, and Tor/WebRTC mismatch cases passed.
- **Build remains blocked by runtime verification.** The current multi-feature checkout has not been packaged or run on Windows. Required evidence: first-window New Tab rendering, F icon at normal scaling, diagnostic view/readbacks, all four mode switches and interruption warnings, slow Tor bootstrap and unexpected exit/proxy recovery, DoH preset/custom readback, Smart Search local search/blocklist/remove and one explicitly keyed Tavily response with quota accounting. A static audit cannot establish live network routing, truth classification, or all Gecko extension API behavior.
- Existing open risks at that checkpoint: GHOST could affect pre-existing site data, mode setting could leave a partial selection, DNS preference readback did not prove resolver egress, and VPN selection could not start an external VPN. See the second audit below for fixes and remaining limits.

## Second conflict audit and fixes

| Flow | Defect found | Source change and evidence |
| --- | --- | --- |
| Tor interruption / restart | Failed proxy or DNS restoration at browser startup discarded the saved Tor ownership and prior settings. Stopping Tor also swallowed DNS restoration failure and discarded recovery state. A bootstrapped process was treated as healthy on repeat start without checking SOCKS routing. | Preserve recovery state until both proxy and DNS restore; report failed routing; retry failed stop/restoration. Unexpected process without saved ownership displays ERROR and offers stop. Mocked proxy/DNS restoration failures and retries pass. Native process-exit scenario still required. |
| Mode switching | A privileged `applyMode` failure could leave the stored mode ahead of effective settings; the sidebar checked `ok` and could visually revert despite a persisted partial selection. | Store mode after settings apply; on thrown failure attempt prior-mode rollback. Return `ok=false` for effective readback mismatch while keeping the actual selected mode visible with warning. Mocked thrown setter and partial readback pass. Individual `safeSet` failures remain visible by effective readback rather than making privacy settings atomic. |
| HTTPS-only vs modes | Mode changes reset the separate manual HTTPS-only switch. | Save manual override and reapply it after mode changes and startup. Mocked preference restoration passes. |
| GHOST cleanup | Site data was removed by hostname without a time bound; cleanup failure erased its retry record. | Combine tracked hostnames with `since: startedAt`; retain session on failure, preventing a successful mode exit until cleanup can be retried. [Mozilla documents these removal options](https://developer.mozilla.org/en-US/docs/Mozilla/Add-ons/WebExtensions/API/browsingData/RemovalOptions); actual storage behavior on packaged Gecko remains to be tested. An existing site's recently created/modified data can still be affected, so GHOST is not a separate ephemeral profile. |
| Tavily daily quota | UTC date reset differed from the user's local day near midnight. | Use the browser's local calendar day for daily/monthly counters. No request is made by quota status or key save. Local date check passes; real API response/quota still untested. |
| Search exclusion | Rechecked host/subdomain matching, immediate hide, counted hidden results, and removal without another paid request. | Mocked add/remove and count pass. Browser UI and provider response still pending. |
| Resource budget / UI | A failed tab discard was counted as though the tab were gone; theme application swallowed its error and reported success. | Count failed discards in active background totals and degradation warning. Theme action now reports application errors; startup still logs a recoverable theme failure. Sidebar rereads actual mode after an ancillary UI read fails. Native visual/runtime confirmation pending. |

Current gate: all extension JS passes syntax check, manifest/schema JSON parse, focused `node scripts/audit-functional.cjs` passes, product validator and diff check pass. No Windows runtime test or new packaged artifact was run. Before proposing a local build, test the exact source in Gecko/Windows for startup/toolbar, all modes and interruption warnings, Tor slow bootstrap/forced exit/egress and recovery, DNS readback, GHOST cleanup semantics, Smart Search rendering and keyed Tavily accounting. The test environment here has no Firefox, Tor, Wine or Windows runner available locally. VPN control remains system proxy routing only, and fake-site marking remains a user decision.

## Independent review follow-up

An external reviewer inspected the source and reported three findings. Confirmed and addressed:

- **P1, startup Tor recovery:** when proxy/DNS restoration failed, saved Tor ownership remained true but initialization reapplied NORMAL/TURBO/PRIVATE WebRTC=true. Initialization now forces WebRTC=false while recovery remains pending. A mocked startup scenario checks that the final value is false. A failed privacy setter is logged and detected by mode health; live leak behavior still needs Windows verification.
- **P2, Tor plus mode transition:** the unguarded WebRTC=false setter intentionally aborts a mode change if it fails, rather than claiming successful mode activation with WebRTC exposed. Added an explicit fail-closed comment and a mocked failed-setter rollback case.
- **P3, GHOST rollback after cleanup:** successful cleanup followed by later mode failure restarts an empty GHOST session. The saved status and sticky sidebar warning now state that the session was restarted. A mocked cleanup/rollback case checks the empty session and warning flag.

The reviewer also confirmed source/schema wiring and Smart Search parsing but did not run Gecko. The review did not change the repository directly. No new build or remote publication.

## Deeper Tor activation gate

The Tor start path had a further mismatch not covered by the external review: failure to disable WebRTC was swallowed, and `torEnabled=true` was persisted immediately after setter calls without effective readback. Tor activation now requires readback of SOCKS5 proxy with remote DNS, DoH off, and WebRTC disabled. Any mismatch aborts activation. If rollback of proxy or DNS fails, the prior snapshot and Tor ownership are retained for recovery rather than reporting a clean OFF state. The periodic sidebar status uses the same effective proxy/DNS/WebRTC checks, so it cannot show `TOR: ON` with WebRTC enabled. Mocked successful activation, readback mismatch, failed WebRTC setter, failed rollback, and retry pass. Windows egress and unexpected process-exit tests remain required.

## Whole-codebase regression pass

Re-read the extension views, background/runtime control, privileged API, packaged asset reconstruction, and Windows build gate after all earlier fixes. New findings and source fixes:

| Area | Inconsistency | Check after fix |
| --- | --- | --- |
| Diagnostics | It labeled Tor routing as configured based on SOCKS alone, while the main TOR button required SOCKS, DoH off and WebRTC off. | Uses the same `get-status.torRouted` verdict and displays actual DoH and WebRTC readings. Wiring gate verifies view references. |
| Search redirect | `URLSearchParams.get('uddg')` was decoded a second time, changing encoded destination paths. | One decode only; regression uses a `%252F` destination. |
| Search replacement | A superseded search could resume after storage loading and issue its explicit Tavily request; its cancellation could overwrite the newer search summary. | Abort check before starting sources; stale catch cannot replace current UI. Interleaved mocked search proves zero paid calls. A request already sent cannot be refunded by Stop. |
| Tor bootstrap UI / concurrency | A legitimately starting process could be labeled unexpected. Simultaneous mode, DNS and Tor messages could interleave. | Distinct `TOR: AVVIO` state, disabled network controls during bootstrap, serialized control transitions. Mocked queue order, failure recovery and sidebar state pass. External proxy edits remain detectable by status readback. |
| GHOST tracking | Host list silently discarded entries after 200. Concurrent tab updates could overwrite the list; storage errors were treated like non-web URLs. | No silent host cap; serialized writes; health warning on failed storage. Mocked 205-host session and failure pass. This still cannot guarantee isolation of subresource storage or a separate ephemeral profile. |
| Library / Addons | Several rejected operations left loading text or silently ignored an error. | Errors are displayed on the page; action promises and initial rendering are caught. Native API behavior still needs user-facing runtime checks. |
| Build gate | New focused tests were not invoked by the Windows workflow. | Product, wiring and functional checks now run before the expensive build. Reconstructed approved background JPEG is 959×540 with expected SHA-256. |

Local gate: `node --check` on every extension JS and audit script, JSON parsing, `python scripts/validate-product.py`, `python scripts/audit-wiring.py`, `node scripts/audit-functional.cjs`, `git diff --check`, and background asset SHA-256 reconstruction pass. This is not a Windows native run. Tor real egress/forced exit, privacy API readbacks in Gecko, page rendering, GHOST cleanup effect, Tavily live API and quota response, and the affected user's slow bootstrap remain release blockers. No build or publication.

Local checks: all extension JavaScript passed `node --check`, experiment schema parsed, `scripts/validate-product.py` passed, and `git diff --check` passed. Mocked message-handler tests verified Tor-active mode switch keeps WebRTC disabled, DNS edits are refused while Tor is enabled, and a subsequently changed fingerprint setting produces a mode-health warning. These checks cannot validate Gecko-specific APIs, rendering, external DNS traffic, or the affected user's Tor bootstrap.
