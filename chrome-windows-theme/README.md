# FILUM Chrome theme

This is a separate optional Chrome theme for the browser frame (tabs and toolbar). Load this folder through `chrome://extensions` with Developer mode enabled. Then load `chromium-extension` separately for the FILUM new tab and side panel. Chrome allows one active theme at a time. The theme cannot resize Chrome tabs or replace native toolbar widgets.
